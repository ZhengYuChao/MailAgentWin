任务：严格按照《邮件批量处理指令》批量处理目标邮件。

【本轮处理数量 N】：2

数据源仅限 Notion 中的《我的邮件｜[Adam] Email》，不得使用外部 Mail 连接。

执行规则：
1. 筛选以下候选邮件：
   - Processing Status = 未处理 / AI Reviewed；
   - 或 Processing Status = 已同步，但必填处理字段不完整。
2. 先形成完整候选集合，按 Date 倒序排列，只处理前 N 封。
3. 同 Thread 的其他邮件仅用于理解上下文，不计入 N，也不得修改。
4. 全自动执行，无需过程确认。处理前读取数据库真实 Schema，严格使用真实字段名、类型和枚举值。
5. 对“未处理”邮件，先标记为 AI Reviewed，再逐封读取完整邮件页面和相关字段。
6. 严格按照《邮件批量处理指令》完成字段判断、状态流转、写入前自检及安全检查。
7. 每封邮件一次性写入全部结果：
   - 未处理 / AI Reviewed → 通过自检后设为 已同步；
   - 已同步但字段缺失 → 补齐后保持 已同步。
8. 不处理“草稿已创建 / 已完成”的邮件；不发送邮件。
9. 只有必填字段完整且 Processing Status = 已同步，才计入完成数量；失败邮件不得伪装为完成。

执行结束后只回复一行，并将 N 替换为本轮实际数字：

全部完成：
邮件（N/N）已处理完毕。

未全部完成：
邮件（X/N）已处理完毕（未完成原因：一句话说明）。