任务：严格按照《邮件批量处理指令》处理 Notion 数据库《我的邮件｜[Adam] Email》中的目标邮件。

【本轮处理数量 N】：2

处理前读取数据库真实 Schema，仅使用真实字段名、类型和枚举值。
候选范围：Processing Status = 未处理 / AI Reviewed；或 Processing Status = 已同步但必填处理字段不完整。排除已有的草稿已创建 / 已完成邮件。
先形成完整候选集合，按 Date 倒序，只处理前 N 封；不得超量处理。
同 Thread 邮件默认仅用于上下文，不计入 N、不修改；但若 Adam 的发件已明确处理历史收件中的请求，允许按指令关闭对应历史收件，联动关闭不计入 N。
未处理邮件入选后先设为 AI Reviewed，再逐封读取正文和相关字段。
每封目标邮件一次性写入全部处理字段和最终状态：
仍需 Adam 行动或仍在等待对方回复：已同步；
明确无需行动、事项已结束或 Adam 已实质处理：已完成；
处理失败：保留 AI Reviewed 或原状态，不得伪装成功。
只有必填字段完整，且最终状态为已同步或符合规则的已完成，才计入完成数量。
仅操作该 Notion 数据库；不连接外部 Mail，不发送邮件，不创建草稿，不删除或归档邮件页面。
全自动执行，无需过程确认。
执行结束后只回复一行，并将 N 替换为本轮实际数字：

全部完成：
邮件（N/N）已处理完毕。

未全部完成：
邮件（X/N）已处理完毕（未完成原因：一句话说明）。