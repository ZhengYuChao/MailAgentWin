任务：按规则批量处理 2 封目标邮件。
请严格遵循《邮件批量处理指令》的规则，全自动执行，无需过程确认。

1. 邮件筛选与处理
在 《我的邮件｜[Adam] Email》这个page中（而不是去连接notion email）筛选目标邮件，范围包括：
- Processing Status = 未处理 / AI Reviewed；
- 或 Processing Status = 已同步，但关键字段缺失。
将以上目标邮件按 Date 倒序排序，仅处理前面提到的邮件数。

处理要求：
- 逐封读取完整邮件内容，包括 Subject、正文、From、To、CC、Date、Thread ID、附件、Flag 等。
- 按《邮件批量处理指令》补齐所有缺失关键字段。
- 最终只允许将 Processing Status 更新为 已同步，严禁写入 已完成。
- 已同步但字段缺失的邮件，补齐字段后保持 已同步。

关键字段包括：
AI Summary、Key Points、Category、Language、Sender Priority、Priority、Action Required、Action Type。
若 Action Required = true，必须补齐 Reply Suggestion。
若 Priority = 💥领导 或 🔴紧急，必须补齐 Urgency Reason。

字段写入防错：
- 写入前必须读取数据库真实 Schema，不得猜字段名。
- 严格使用真实字段名，尤其注意含空格字段：AI Summary、Action Required、Action Type、Sender Priority、Processing Status。
- 每封邮件更新后必须重新读取页面校验。
- 只有关键字段完整且 Processing Status = 已同步，才计入完成数。

2. 极简结果输出
执行结束后，只回复一句话，严格使用以下格式：
邮件（X）已处理完毕。
如果未完成，请在括号内用一句话说明原因。